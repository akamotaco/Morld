# S02 연애 요소 확장 설계

> 목표: S02에서 era TW 수준의 풍부한 연애/성적 시뮬레이션 구축 → 완성 후 S04로 이식.
> 참고 소스: `c:\tmp\era-source\TWKR-Textbung` (한국어 포크, 원본 eratohoTW 기반).
> 방향: era를 축소한 subset이 아닌, **NTR/NTL/플레이어 피해 경로 포함한 superset**.

---

## 설계 원칙: 3축 능력치 (공격/방어/내성)

**캐릭터 능력치는 역할별로 분리 — 중복 기능 금지**:

| 축 | 성격 | 주 사용처 |
|----|------|---------|
| **근력** (`근력` prop) | 공격적 | 강간(`resolve_action_mode` forced 판정), 물리적 제압 |
| **체력** (`생존:체력` prop) | 방어적 | 탈출(`calculate_escape_chance`), 저항 지속 |
| **자제심** (`성격:자제심` prop) | 정신 내성 | 트랜스 진입 방어, 세뇌/약물 저항, 수치심 모디파이어 |

**의미**: 각 축은 독립적인 "당하는 방식"에 대응 — 물리/지속/정신. 한 축을 키우면 다른 축은 상대적으로 약해지는 트레이드오프 가능 (Phase 2 탤런트에서 확장).

---

## 0. 핵심 설계 원칙 (기획 우선)

> 이 원칙들은 Phase 계획보다 상위에 있으며, 모든 구현 결정을 제약한다.

### 0.1 단일 시스템 원칙 (Unified Base)

**모든 NPC는 동일한 스탯/행위/게이트 구조를 공유한다.**

- 랜덤 생성된 Tier 3 NPC부터 핵심 S02 8인 Tier 1까지 **스탯 스키마 동일**
- 게이트 공식 (3-path: 호감/복종/성욕) **동일하게 적용**
- 31개 궤적 아크타입 **전원 진입 가능**
- 액션 카테고리 (애무/도구/삽입/SM/강제/데이트/일상) **전원 접근**

**금지**: Tier별로 별도 로맨스 시스템을 만들지 않는다. 베이스는 단 하나.

### 0.2 Tier 오버라이드 증강 모델 (Augmentation, not Replacement)

**Tier 차이 = "커스터마이즈 깊이"**, 시스템 차이가 아님.

| Tier | 대사 | 이벤트 | 탤런트 | 퀘스트 |
|---|---|---|---|---|
| **Tier 3** (랜덤) | 아키타입 톤 템플릿 자동 생성 | 스탯 기반 자동 트리거만 | 아키타입 기본값 | 없음 |
| **Tier 2** (네임드 조연) | Tier 3 + 고유 대사 일부 override | 고유 이벤트 몇 개 추가 | 고유 탤런트 조합 | 소규모 |
| **Tier 1** (핵심 8인) | Tier 2 + 풀 오리지널 대사 | 전용 이벤트 시나리오 | 세밀 튜닝 | 주요 스토리 |

**패턴**: override lookup → 없으면 Tier 3 베이스로 fallback. (현 Hybrid 대화 엔진의 Tier 구조와 동일)

- 랜덤 NPC도 **31개 궤적 모두 자연 발생** (대사만 아키타입 톤)
- Tier 2+ NPC는 **독점 대사/이벤트로 개성 부여**, 하지만 시스템은 동일
- **신규 Tier 1 추가 = 기존 베이스 + override 파일** (시스템 재작성 아님)

### 0.3 역할 대칭 원칙 (Symmetry)

**플레이어와 NPC는 역할 구분 없이 subject/object 교환 가능**.

- 플레이어에게 NPC가 가진 모든 스탯 존재 (성욕/수치심/자제심/경험/처녀/탤런트 등)
- AI 행동 루프 역방향 작동 (NPC가 플레이어를 대상으로 think)
- 게이트 공식은 actor/target 매개변수만 받음 (플레이어 하드코딩 금지)

### 0.4 era TW는 참고, morld는 superset

**축소하지 않고 확장한다.**

- era TW 검증된 설계 요소 차용 (자제심/수치심/성별기호/각인 등)
- era에 없는 영역 적극 도입 (NTR/NTL/플레이어 피해/시뮬 자생 아크타입)
- 31개 궤적 중 era 직접 대응은 ~15개, 나머지 16개는 morld 고유 확장

### 0.5 기획 우선, 구현 차후

**올바른 기획 없이 코드 수정하지 않는다.** 현재 우선순위:

1. ✅ 설계 문서 (era 분석 / 확장 계획 / 궤적 공간)
2. ⏳ 스탯 네임스페이스 확정
3. ⏳ 아키타입 ↔ 탤런트 기본값 매핑
4. ⏳ Tier 오버라이드 포맷 명세
5. → **이후** Phase 0 구현 진입

---

## 1. era TW 소스 분석 요약

### 1.1 스탯 계층 (CSV 기반)

```
Palam.csv — 관계 파라미터 (쾌감/공순/욕정/굴복/고통/호의/반감 등)
Base.csv  — 독립 신체 상태 (체력/기력/정력/이성/분노/신장 등)
Abl.csv   — 능력/성향 (감각/친밀/순종/봉사정신/노출벽/마조끼/새드끼/레즈끼/호모끼 + 중독 계열)
Talent.csv— 탤런트 (자제심/수치심/정조/저항/성별기호/새드/마조/질투 등 1075줄)
Mark.csv  — 영구 각인 (고통각인/쾌락각인/반발각인/시간각인/성장)
Stain.csv — 체액 얼룩 (부위별 비트 플래그)
ex.csv    — 절정 상태 (C/V/A/B/M 절정 + 이중/삼중/사중/오중절정)
exp.csv   — 경험 누적 (부위별 + 행위별 + 체위별 + 조교경험 등)
Train.csv — 행위 커맨드 정의 (370줄, 카테고리별 번호 체계)
Equip.csv — 의복 슬롯 (상반신/하반신/속옷/외투 등)
```

### 1.2 era TW에 이미 존재하는 스탯 (morld 설계와 매칭)

**자제심 · 수치심은 이미 Talent로 존재**:
- Talent 20: **자제심** (매각 시 욕망 4 이상 필요, 굴복/쾌락계 오르기 어렵고 반감 오르기 쉬움)
- Talent 33: **수치심** (-1 수치심 없음 / 1 부끄러워함, 노출계 조교 효과 변동)

→ morld가 제안한 이중 축이 era TW에서 이미 검증된 설계.

**관련 성향 탤런트** (morld 아키타입과 자연 매핑):
- 10 담력, 11 태도, 12 응답, 13 자존심, 14 츤데레
- 30 정조, 31 자기애, 32 저항, 34 약점
- 70 쾌감응답, 72 즉각함락, 73-77 부위 편향 (음호/항문광/음유/키스마)
- 80 도착적, 81 성별기호, 82 새드, 83 마조, 85 망신, 86 소악마
- **84 질투 (정의만 있고 미구현)** ← morld가 채울 빈 영역

### 1.3 성별기호 (동성애 처리 모델)

Talent 81 단일 값으로 관리:
- `-1` 양도(bi) → 레즈비언 조교 허용, 레즈끼/레즈중독 상승
- `0` 기본(이성애)
- `1` 남성기피
- `2` 여성기피
- `3` 인간불신

연관 ABL: 17 레즈끼 / 18 호모끼 / 32 레즈중독 / 33 게이중독
연관 Exp: 51 레즈경험 / 52 게이경험

→ **성적지향을 별도 시스템이 아닌 "성향 탤런트 + 중독/경험"의 조합**으로 처리.

---

## 2. 발생 가능 상황 카테고리 (Train.csv + ERB 분석)

### 2.1 단일 참가자 (Solo)

| 번호 | 커맨드 | 비고 |
|---|---|---|
| 9/451 | 자위 | 도구별 변형 (40-49 로터/마사지기/바이브 등) |
| 183 | 비디오촬영 | 자기 촬영 |
| 402/417 | 취침/낮잠 | 회복 |

### 2.2 1:1 합의 — 애무/스킨쉽 계열 (0-24)

| 카테고리 | 행위 |
|---|---|
| 애무 | 애무/커닐링구스/펠라/손가락삽입/애널핥기·애무 |
| 가슴 | 가슴애무/유두자극·빨기·주무르기/보지 벌리기/클리애무 |
| 키스/대화 | 키스/키스(계속)/유혹/필로토크 |
| 공수교대 | 24: 공격/수비 전환 |

### 2.3 1:1 도구 계열 (40-51)

로터/E마사지기/클리캡/바이브/애널바이브/유두캡/착유기/콘돔/피임약

### 2.4 1:1 삽입 계열 (60-79)

**체위**: 정상위/후배위/기승위/대면좌위/배면좌위 + 각 A변형
**특수**: 역강간(64)/양구멍삽입(71)/G스팟·자궁구·S형결장·질내자극/난쟁이오나홀

### 2.5 1:1 펠라/파이즈리/수음 (80-96)

수음/펠라/파이즈리/스마타/거품춤/풋잡/진공펠라/콘돔정액먹기 + 상대 주도 파생

### 2.6 SM (100-108)

스팽킹/채찍/촛농/바늘/눈가리개/밧줄/구속플레이/젖스팽킹

### 2.7 강제 계열 (120-150)

강제: 커닐링구스강제/보지비비기/풋잡한다/수음한다/애무받는다/젖비비기/이라마치오/배에강펀치/가슴골 범하기/당하는 체위 변형(130-133)

### 2.8 특수 도구/상황 (180-192)

로션/미약/이뇨제/비디오촬영/야외플레이/수치플레이/샤워/새댁플레이/수면제/배란유발제

### 2.9 데이트/일상 (300-380)

회화/차를 탄다/스킨십/사과/일 돕기/무릎베개/볼 잡기/머리쓰다듬기/엉덩이쓰다듬기/끌어안기/키스/가슴·애널·클리애무
상대주도정상위·후배위 + 로터 삽입
덮친다/데리고나간다/고백/장난/동침/시간정지/그늘로꾀다/섹프계약/프로포즈

### 2.10 일상 활동 (400-500)

이동/콜렉션/취침/휴식/일기장/청소/전투훈련/공부/요리/식사/연주/텃밭/목욕
기념품 구매/의뢰 수행/매춘

### 2.11 관계 특수 (603-699)

손잡기/산책/외식/꽃집/술집/낚시/돌쌓기 경연/노름판/방 방문/들어가기

### 2.12 **다인조교 (3인 이상 동시 참가)**

`ERB/다인조교/나눠먹기.ERB` 분석:

**메커니즘**: **부위별 점유 추적**
```
몸사용중[MASTER][부위] = {
    사용정도:  0 (없음) / 1 (단일) / 2 (이중)
    사용자:    누가 사용하는지 (다수 가능)
}
```

**추적 부위**: 입 / 손 / C(클리) / V(질) / A(항문) / B(가슴) / P삽입

**동시 참가 모델**:
- 한 대상에 여러 참가자가 **서로 다른 부위 동시 점유**
- 일부 부위(ex: 가슴)는 두 명이 한쪽씩 점유 (사용정도 2)
- 삽입 부위(V/A/P)는 기본 단일 (도구 병용 제외)
- TFLAG로 전역 상태 추적 (MASTER의입사용중/손사용중/...)

→ **3인 이상 시나리오는 "한 중심 대상 × 여러 참가자의 부위 분담" 구조**로 모델링.

### 2.13 동성 행위 (ERB 분석)

- `CFLAG:LOCAL:우후후` 상태에서 진행
- 반환값 1 = 동성(레즈비언) 판정
- 커맨드 자체는 동일 (애무/삽입 도구 등)
- 성별기호에 따라 커맨드 실행 용이도/효과 변화
- 경험치 항목 분리 (레즈경험/게이경험)

→ **행위 커맨드는 성별 무관 통합, 효과 계산 시 성별기호로 분기**.

### 2.14 감금/매음/강제 (감금관련/지하감옥/지하매음굴/잠복모드)

- 감옥 노역/뇌물 (752-753)
- 매음굴 알바/카운터/개인실 (761-764)
- 잠복모드 (범죄 플레이어 상태)
- 감금실 설정 → 장기 조교 시나리오

---

## 3. NPC↔NPC + NTR/NTL 관련 소스 단서

### 3.1 현재 era TW 지원
- **질투 탤런트 정의만 있음, 미구현**
- **ANOTHER_TALK.ERB**: NPC 간 대화/이벤트 일부
- NPC 상호 친밀 관계는 있으나 연애/성적 상호작용은 얕음

### 3.2 morld가 확장할 영역
- 질투 탤런트 구현
- 충성심 (파트너가 타 NPC에게 접근 시 저항도)
- NTR 서사: 증거(비디오촬영)/발각/마음 균열
- NTL 서사: 명시적 허용 프로토콜
- 플레이어 피해: 역방향 지배 관계 + 플레이어 스탯 풀세트

---

## 4. S02 현황 대비 갭

### 4.1 이미 존재 (era TW 대응 양호)
- `관계:{p}:호감` ≈ Palam 호의/친밀
- `관계:{p}:복종` ≈ Palam 공순/굴복
- `관계:{p}:반발` ≈ Palam 반감/불쾌
- `상태:성욕` ≈ Palam 욕정
- `경험:{부위}` ≈ exp C/V/A/B/M 경험
- `처녀:{부위}` ≈ CFLAG 처녀
- `생존:체력` ≈ Base 체력
- 임신/월경 ≈ 임신관련 ERB

### 4.2 결정적 결핍 (도입 필요)
- **자제심** (Talent 20) — 내면 억제 축
- **수치심** (Talent 33) — 사회 억제 축
- **성별기호** (Talent 81) — 동성/이성 분기
- **질투** (Talent 84) — NTR 핵심
- **성향 탤런트 체계** — 새드/마조/도착적/즉각함락/자위하기쉬움 등
- **각인 시스템** (Mark.csv) — 영구 성격 변화
- **부위별 점유 모델** — 다인 시나리오 기반
- **부위별 경험 세분화** — 현재는 경험치 있으나 체위별/상황별 분리 없음

### 4.3 미구현 확장 (morld 고유)
- NTR/NTL 경로 (era도 구현 안 됨)
- 플레이어 대칭 스탯 풀세트
- 관찰자 시스템 (제3자 목격 이벤트)

---

## 5. S02 구현 계획 (Phase 1~6)

### Phase 0: 즉시 수정 (당장 실행)
**`관계:{p}:욕망` 완전 제거**
- 게이트: `상태:성욕`(독립)으로 이관
- 할인: `get_effective_affection_req` 파라미터 재정의
- 이벤트 modify_prop: `상태:성욕`으로 리다이렉트
- 디버그 버튼: 라벨 정리 ("상태성욕 +20")
- 테스트 및 문서 업데이트
- 예상 영향: 14 참조처 수정

### Phase 0.5: 공식 단순화 + 점수 합산 인프라 ✅ (2026-04-22 완료)

애정행위와 강제 행위 통합을 위한 기반 작업. era TW의 `GET_SUCCESS_RATE` 패턴에서
차용한 **"베이스라인 + 모디파이어 점수 합산"** 모델을 도입.

**변경**:
- **제압 공식 단순화**: `calculate_force_chance` — 근력차만 (체격/hp 제거)
- **탈출 공식 재작성**: `calculate_escape_chance(target, actor)` —
  체력차 베이스라인 + 복종/반발/성욕 모디파이어
- **`is_futile`(항상실패) 제거**: 향후 `physical_req`(근력 greyed out)로 치환
- **절정 페널티 제거**: 향후 연쇄 절정(자제심/각인 연계)으로 치환
- **점수 함수 추가**: `calculate_availability_score(partner, player, action)`,
  `resolve_action_mode` → `"consensual" | "forced" | "unavailable"`
- **`is_action_available`**: shim으로 유지 (resolve_action_mode == "consensual")

**설계 원칙**:
- 베이스라인(단순 고정 공식) + 모디파이어(성격/상태/각인 가감)의 2레이어
- 각인/자제심/탤런트는 이후 Phase에서 모디파이어로 "추가만" 하면 됨
- 행위 게이트는 점수 ≥ 0 → consensual / < 0 → forced / physical_req 불충족 → unavailable

**Phase 0.6 완료** (2026-04-22):
- ✅ `physical_req` 스키마 도입 — action_def의 선택적 필드 (`strength_advantage`, `min_strength`)
- ✅ `check_physical_req(action, partner, player)` 헬퍼 + `resolve_action_mode` 통합
- ✅ UI greyed out 렌더링 (근력 부족 시 "(근력 부족)" 접미사)
- ✅ consensual 세션에서 "강제 {name}" 적색 클릭 옵션 (호감 미달 시)
  - 클릭 시 일회성 `force_instant:` / `force_toggle:` 디스패치 → 모드 스왑 + 복원
  - NPC 저항 체크/반발 누적은 forced 액션마다 발생 (혼합 세션)
- ✅ harassment 액션 카탈로그 흡수 (slice 3, 2026-04-22)
  - 8개 액션(lift/tear/grope)이 `INSTANT_ACTIONS`에 `forced_only: True` + `harassment_exec` 태그로 진입
  - `harassment.execute_lift/tear/grope`는 `action_def` dict 수신 형태로 리팩터 (기존/신규 양쪽 필드 지원)
  - romance.py 즉시 행위 디스패치가 `harassment_exec`일 때 전용 파이프라인 실행
  - `base.harass()` → `start_romance(mode=MODE_FORCED)` 단일 진입점으로 통합
  - `harassment_session` 루프 제거 (dead code). `combat_harass` 호환 유지.
  - `forced_only` 액션은 consensual 세션 UI에서 숨김

### Phase 1: 억제 이중축 도입
**자제심 × 수치심**
- 신규 prop: `성격:자제심` (0-100), `상태:수치심` (0-100)
- 기본값: 아키타입별 설정 (순수/절제 → 자제심 고, 방종 → 자제심 저)
- 게이트 공식:
  ```
  action_threshold = base
                     + 자제심 × α (내면)
                     + 수치심 × β × 관객계수 (사회)
                     - 성욕 × γ (촉진)
  ```
- 관객계수: location 내 타 NPC 존재 여부 + 관계 친밀도로 계산

**Phase 1 Slice 1 완료** (2026-04-22):
- ✅ `get_restraint_modifier(partner_id)` — 자제심 × -0.3
- ✅ `get_audience_factor(partner_id)` — 제3자 수 / 공간 길이 × 시야 배율 × (1 - 은신률)
- ✅ `get_shame_modifier(partner_id)` — 수치심 × -0.2 × 관객계수
- ✅ `calculate_availability_score`에 두 모디파이어 합산 통합
- ✅ 17 신규 테스트 (TestRestraintAndShame / TestAudienceFactor / TestShameModifier)

**관객 계수 공식** (Slice 1.5 정교화, 2026-04-22):
```
density = min(1.0, 제3자_수 / location.length)
visibility_mult = 1.0 (실내) / 1.2 (야외)
stealth_chance = 0.3 (기본) / 0.7 (status:stealth=1)

factor = density × visibility_mult × (1 - stealth_chance)
```

예시 (수치심 100 기준 페널티):
- 관객 없음 → 0
- 좁은 실내 (length=1) + 비은신 관객 1명 → -14
- 넓은 거실 (length=5) + 비은신 관객 1명 → -2.8
- 야외 + 비은신 관객 1명 → -16.8
- 좁은 실내 + 은신 관객 1명 → -6

**Phase 1 Slice 2 완료** (2026-04-22):
- ✅ 11 아키타입 × 자제심 기본값 매핑 (`ARCHETYPE_RESTRAINT_DEFAULT`)
- ✅ `_get_partner_archetype()` — prop / instance / 성격 매핑 fallback 체인
- ✅ `get_restraint_value()` — 명시 prop 우선, 아키타입 기본값 fallback, 없으면 0
- ✅ 9 신규 테스트 (TestArchetypeRestraintDefaults)

**자제심 기본값 표**:
| 아키타입 | 기본값 | 의미 |
|---------|-------|------|
| innocent | 80 | 순진, 강한 내면 억제 |
| timid | 70 | 소심, 꺼림 |
| cold | 70 | 냉담, 거리감 |
| proud | 65 | 오만, 자존심 |
| tsundere | 60 | 겉 억제 |
| devoted | 55 | 파트너에만 개방 |
| stoic | 50 | 과묵, 중립 |
| gentle | 40 | 온화 |
| fierce | 30 | 욕구 강함 |
| cheerful | 25 | 사교적/개방 |
| seductive | 10 | 방종 |

**Phase 1 Slice 3 완료** (2026-04-23):
- ✅ `apply_shame(unit_id, delta)` — 0~100 clamp + prop 반영
- ✅ `on_romance_discovered(partner)` — 행위 중 발각 (+20, 나체 +10)
- ✅ `on_stealth_near_miss(partner)` — 은신 스릴 (+3)
- ✅ `on_masturbation_witnessed(unit)` — 자위 목격 (+25)
- ✅ `on_nude_in_public(unit)` — 공공 노출 진입 (+5)
- ✅ npc_initiative.py 발각 경로에 `on_romance_discovered` 훅 추가
- ✅ romance.py near_miss 경로에 `on_stealth_near_miss` 훅 추가
- ✅ 10 신규 테스트 (TestShameEventHooks)

**수치심 이벤트 수치 (임시 밸런스)**:
| 이벤트 | 증가량 | 트리거 시점 |
|-------|-------|------------|
| romance_discovered | +20 (+10 if nude) | `advance_time_and_check` 발각 시 |
| stealth_near_miss | +3 | 은신 roll 성공 시 |
| masturbation_witnessed | +25 | (훅 정의, 호출 지점 미구현) |
| nude_in_public | +5 | (훅 정의, 호출 지점 미구현) |

**Phase 1 Slice 4 완료** (2026-04-23):
- ✅ romance_ui 스탯 패널에 자제심/수치심 표시 (값 > 0일 때만)
  - 예: `자제심: 50  수치심: 30 (관객 ×0.7)`
- ✅ `_SHAME_REGISTRY` 기반 자연 감쇠 (1시간당 -5)
- ✅ `subscribe_time_elapsed` 구독 — 모듈 로드 시 자동 등록
- ✅ 3 신규 테스트 (감쇠 tick / 0 clamp / 레지스트리 추적)

**Phase 1 완료 상태 (2026-04-22 ~ 2026-04-23, 5 슬라이스)**:
- Slice 1: 자제심/수치심 모디파이어 인프라 (11 테스트)
- Slice 1.5: audience_factor 정교화 (밀도 × 시야 × 은신) (6 테스트)
- Slice 2: 아키타입 → 자제심 기본값 매핑 (9 테스트)
- Slice 3: 수치심 이벤트 훅 (apply_shame + 트리거) (10 테스트)
- Slice 4: UI 표시 + 자연 감쇠 (3 테스트)

**아직 남은 것**:
- 자위 목격/공공 노출 트리거 지점 실제 연결 (훅만 정의됨)
- Phase 2 탤런트 시스템
- 연쇄 절정 ↔ 탈출 공식 연계 (별도 TODO)
- 정신 교란 상태 (술/약물/최면) 도입 (별도 TODO)

---

## 9. 4단계 상호작용 대칭 (2026-04-23 정리)

스킨십/애정행위/성추행/강제 행위의 구분을 명확히 하고 대칭 구조로 재편.

| 수준 | 동의 (높은 호감) | 비동의 (낮은 호감) |
|------|-----------------|-------------------|
| **단발/경량** | 스킨십 `casual_affection()` | 성추행 `harass()` → `harassment_session` |
| **풀 세션 (삽입/절정)** | 애정행위 `romance()` → `start_romance(CONSENSUAL)` | 강제 행위 `force_romance()` → `start_romance(FORCED)` |

**Phase 0.6 Slice 3 롤백 (2026-04-23)**:
- 당시 `harass()` → `start_romance(FORCED)` 로 묶었으나 `force_romance`와 동일해지는 문제
- 경량 `harassment.harassment_session` 복원 → 짧은 비합의 루프 (lift/tear/grope + 탈출 roll)
- 풀 강제 세션은 `force_romance`로 분리 유지
- 성추행 설정 토글 제거 — `is_romance_enabled`로 통합 (`is_harassment_enabled`는 alias)

**설정 통합 (2026-04-23)**:
- `settings.is_harassment_enabled` → `is_romance_enabled()`의 alias (하위호환)
- `set_harassment_enabled` 제거
- UI 성추행 토글 제거
- `set_romance_enabled`가 `can:harass`/`can:self_expose` prop도 함께 관리

### Phase 1.5: 애정 티어 + 라벨 alias + 선호 체위 대사 (2026-04-23 완료)

**배경**: 강제-함락 루트 테스트 설계 중 발견된 갭 3건을 묶어 반영.

#### 1.5.1 애정 티어 도입 (era `愛情` 정합)
- 신규 prop: `관계:{p}:애정` (0~100)
- 호감과 독립 축 (era 정석): 노예 경로(복종 高)와 연인 경로(애정 高) 스탯으로 분리
- **게이트**: 복종 ≥ `LOVE_BLOCK_SUBMISSION`(60)이면 `modify_love(positive)` 차단
  - 음수 delta는 통과 (강제 종료 페널티 등)
- **강제 종료 페널티**: `apply_forced_end_penalty`가 호감과 함께 애정도 -5~-15 감소
- 구현: `romance_dynamics.py` (`get_love_key`, `get_love`, `modify_love`)

#### 1.5.2 수치 → 라벨 alias
수치 기반 시스템을 유지하면서 네이밍 조회 함수 제공.

| 스탯 | 구간 → 라벨 |
|------|-----------|
| 호감 | 0~19 무관심 / 20~39 지인 / 40~59 친구 / 60~79 신뢰 / 80+ 친애 |
| 복종 | 0~29 자유 / 30~59 순응 / 60~79 충성 / 80~99 복속 / 100 절대복종 |
| 애정 | 0~19 무 / 20~39 호의 / 40~59 애정 / 60~79 사랑 / 80+ 헌신 |

- 구현: `get_affection_label/get_submission_label/get_love_label` (순수 함수)

#### 1.5.3 관계 라벨 파생 (`get_relationship_label`)
호감/복종/애정/반발 조합 → 관계 라벨. **prop 저장 X**, 매 조회 시 파생.
era의 관계 라벨 "고정" 방식과 달리 수치가 움직이면 라벨도 자동 변동.

| 조건 | 라벨 |
|------|------|
| 반발 ≥ 60 | **적대** |
| 애정 ≥ 80 + 호감 ≥ 60 | **배우자** |
| 애정 ≥ 60 + 호감 ≥ 60 + 복종 < 60 | **연인** |
| 복종 ≥ 60 + 애정 ≥ 40 | **헌신적 종자** (함락 후 사랑 각인) |
| 복종 ≥ 60 | **종복** |
| 호감 ≥ 40 | **친구** |
| 호감 ≥ 20 | **지인** |
| 그 외 | **타인** |

#### 1.5.4 NPC 선호 체위 요구 대사 훅 (`npc_position_request`)
선호 체위 시스템은 이미 존재 (`SEXUAL_PREFERENCES.preferred_positions`, `get_preference_mult` 1.2x 배율).
**누락 보완**: 삽입 중 NPC가 선호 체위를 "요구하는 대사" 트리거.

- 조건: 삽입 중 + 현재 체위 ≠ 선호 + NPC 성욕 ≥ 70 + 쿨다운 5분 + 확률 30%
- 출력: 대사만 (체위 변경은 플레이어 선택) — NPC 통제력 유지
- 대사 키: `npc_position_request:{pos_id}` (체위별 특화), fallback `:start`
- 구현: `romance.py::_check_npc_position_request`, `_try_npc_thrust_after_action` 호출 체인
- 6개 주요 캐릭터에 선호 체위별 대사 풀 추가 (lina/yuki/ella/sera/mila/faye)

**설계 배경**: NPC 주도 모드는 별도 다이얼로그 시스템이므로 여기선 대사만 추가.
강제-함락 후 자발성 확장 옵션 중 "NPC 자동 체위 전환"은 복잡도 상 제외.

---

### Phase 1.6: NPC 자율 행위 루프 (2026-04-24 완료)

**배경**: 강제-함락 루트 테스트 중 발견된 "함락 후 자발성" 갭의 일부.
플레이어 주도 세션의 휴지 중에 NPC가 성욕/애정에 따라 스스로 봉사/자위/휴식을 번갈아 수행.

#### 1.6.1 구조 (`npc_thrust_trance` 일반화)

**state**: `state["npc_autonomy"] = {active, current_action, duration_remaining, total_turns, last_exit_elapsed}`

**턴 루프** (플레이어 행위 직후 `_try_npc_autonomy_after_action`에서 호출):
- inactive: 진입 조건 체크 → 확률 통과 시 첫 행위 선택 + 대사
- active: 종료 조건 체크 → 지속 효과 적용 → 잔여 턴 소진 시 다른 행위로 전환

**진입 조건**:
- 삽입 없음 (삽입 중이면 thrust_trance 쪽)
- NPC 성욕 ≥ 80
- 복종 ≥ 60 **OR** 호감 ≥ 70 (함락 또는 순애 적극성)
- 쿨다운 5분, 확률 35%

**종료 조건**: 삽입 시작 / 성욕 < 60 / 최대 20턴 / 플레이어가 fellatio·penis_rub 수동 토글

#### 1.6.2 행위 카탈로그 (`_NPC_AUTONOMY_CATALOG`)

| kind | action | 부위 태그 | 가드 |
|------|--------|---------|------|
| service | `fellatio` | — | 플레이어 하체 노출 + facing≠back |
| service | `penis_rub` | — | 플레이어 하체 노출 |
| self | `self_breast` | B | 팔 자유 + NPC 상체 노출 + 해부학 B |
| self | `self_nipple` | B | 동상 (유두는 SENSATION_MAP에서 B로 매핑) |
| self | `self_clit` | C | 팔 자유 + NPC 하체 노출 + 해부학 C (여성) |
| self | `self_vaginal` | V | 팔 자유 + 하체 노출 + 해부학 V |
| self | `self_anal` | A | 팔 자유 + 하체 노출 + 해부학 A (남녀 모두) |
| rest | `rest` | — | 없음 |

#### 1.6.3 가중치 공식

- **service**: 고정 1.0
- **rest**: `max(0.1, (100 - arousal) × 0.02)` — 성욕 높을수록 휴식 확률 감소 (100→0.1, 80→0.4, 60→0.8)
- **self**: base 1.0 × (`preferred_parts` 포함 시 ×2.0) × (`sensation_level ≥ 3` 시 ×1.5)

#### 1.6.4 대사 풀 (6 캐릭터 × 10 키)

각 캐릭터에 아키타입 톤별:
- `npc_autonomy:start` / `npc_autonomy:switch` (공통 진입/전환)
- `npc_autonomy_{action}:start` (행위 8종별 특화 대사)

#### 1.6.5 테스트 커버리지

신규 31개 e2e 테스트:
- **카탈로그 구조** 9개 (`TestAutonomyFramework`, `TestAutonomySelfStimCatalog`)
- **대사 풀 존재** 4개 (`TestAutonomyDialoguePool`)
- **가드 동작** 6개 (`TestAutonomyGuard` — 결박/해부학/facing)
- **가중치 공식** 7개 (`TestAutonomyWeight` — 선호/성욕/rest 곡선)

**설계 원칙 준수**:
- NPC 주도 모드(다이얼로그 시스템)와 별개의 **배경 루프** — 플레이어 통제 유지
- 대사만 출력, 체위 변경/행위 선택은 플레이어 몫 (phase 1.5 position_request와 동일)
- Phase 1.5 애정 게이트와 직교

---

### Phase 1.6.1: 트랜스 상태 통합 (2026-04-24 완료)

**배경**: Phase 1.6 autonomy 진입 게이트를 "복종 OR 호감"으로 두었으나, 사용자 지적 —
**함락이 아니어도 성적 흥분이 충분하면** 자발 행동이 자연스럽다. 기존
`npc_thrust_trance` 진입도 성욕만 본다. 이를 **통합된 트랜스 상태**로 재편.

#### 1.6.1.1 `상태:트랜스` prop (0~100)

산출 공식 (비대칭 방어 전용 — Phase 1.8.1에서 개정):
```
base = (성욕 + 절정게이지) / 2
defense_factor = max(0.1, 1.0 - max(0, 자제심 - 50) × 0.02)
base × defense_factor
+ 트랜스:외부   # 외부는 자제심 우회
→ clamp 0~100
```

**자제심 방어 표**:
| 자제심 | factor | 방어율 |
|-------|-------|------|
| 0~50 | 1.0 | 없음 |
| 70 | 0.6 | 40% |
| 80 | 0.4 | 60% |
| 100 | 0.1 | 90% |

설계 원칙: 자제심은 **정신 공격 내성** 축. 높으면 자연 흥분에 의한
트랜스 진입을 강하게 방어하지만, 강한 외부 자극(세뇌·약물)은 우회 가능.

임계값:
- **트랜스 ≥ 60 (TRANCE_ENTRY)**: 흥분 트랜스 — 페르소나 억제 풀림
- **트랜스 ≥ 80 (TRANCE_DEEP)**: 깊은 트랜스 — 자발성 극대 (저항 약화는 후속)

#### 1.6.1.2 `트랜스:외부` 가산 축 (스켈레톤)

정신 교란 상태(술/약물/최면/세뇌)를 **통합 축**으로 처리하기 위한 공용 prop.
현 단계에서는 prop 정의만 도입 (실제 buff 부여 로직은 후속).
Option B (통합 축) 설계 결정을 완료.

#### 1.6.1.3 기존 시스템 통합

**`npc_thrust_trance` 진입** (romance.py):
- 트랜스 ≥ 60이면 `entry_arousal` −20, `entry_gauge` −10
- 자제심 낮은 NPC (seductive 등)는 낮은 성욕에서도 자연스럽게 진입

**Phase 1.6 autonomy 진입**:
- **복종/호감 게이트 완전 제거**
- 새 게이트: 트랜스 ≥ 60 + 쿨다운
- 의미: 관계 라벨과 무관하게 성적 흥분이 충분하면 자발 행동

#### 1.6.1.4 함수 API (`romance_dynamics`)

| 함수 | 용도 |
|------|------|
| `compute_trance_level(unit_id)` | 수치 계산 (저장 X) |
| `update_trance_level(unit_id)` | 재계산 + prop 저장 |
| `is_in_trance(unit_id, threshold=60)` | 진입 임계 조회 |
| `is_in_deep_trance(unit_id)` | 깊은 트랜스 조회 |

#### 1.6.1.5 테스트 커버리지

신규 11개 dynamics 테스트 (`TestTranceLevel`):
- 공식 검증 (baseline/자제심 영향/외부 가산/clamp)
- 저장 분리 (compute는 저장 X, update만 저장)
- 임계값 판정 (TRANCE_ENTRY / TRANCE_DEEP)

---

### Phase 1.6.2: 삽입 요구 대사 (2026-04-24 완료)

**배경**: 기존 `trance_insert:start`는 **트랜스 중 자동 삽입** 시 사용되는 대사.
삽입 없는 휴지 상태에서 NPC가 "삽입해달라"고 요구하는 별도 훅은 누락.

#### 1.6.2.1 `_check_npc_insertion_request` (대사만)

**조건**:
- 삽입 없음
- NPC 트랜스 ≥ 60 (Phase 1.6.1 공식)
- 해부학 호환: NPC V 또는 A 보유 + 플레이어 P 보유
- 플레이어 하체 노출
- 쿨다운 5분, 확률 25%

**부위 선택 (가중치)**:
- NPC `preferred_parts`에 V 있으면 질 가중치 ×2
- 마찬가지로 A도 ×2
- 양쪽 가능하면 가중치 기반 랜덤 선택

**대사 키**:
- `npc_insertion_request:vaginal`
- `npc_insertion_request:anal`
- fallback `npc_insertion_request:start`

**통합**: `_try_npc_autonomy_after_action` 말미에서 `autonomy` inactive일 때만 체크.
자발 행동(봉사/자위) 중엔 삽입 요구 생략 — noise 최소화.

#### 1.6.2.2 대사 풀 (6 캐릭터 × 2 부위)

각 캐릭터 아키타입 톤별:
- 리나 "...안에 넣어줘~♡" / "...뒤에도 해줘~"
- 유키 "...안에... 넣어주세요..." / "...뒤쪽 괜찮다면..."
- 엘라 "...빨리. 삽입해." / "...뒤로. 넣어."
- 세라 "...삽입해." / "...뒤로. 깊이."
- 페이 "...넣어. 당장." / "...뒤쪽도 괜찮아. 넣어."
- 밀라 "...안에 채워주세요...♡" / "...뒤쪽도 부탁해요..."

#### 1.6.2.3 테스트

e2e `test_all_have_insertion_request_pools`: 6 캐릭터 × 2 키 존재 검증.

---

### Phase 1.7: 수치심 이벤트 훅 호출 지점 연결 (2026-04-24 완료)

Phase 1 Slice 3에서 정의된 훅이 호출 지점 미구현 상태였음. 실제 게임플레이에서
발동되도록 연결.

#### 1.7.1 `on_masturbation_witnessed` — 자위 발각 시

**위치**: `think/handlers/self_comfort.py::_handle_self_comfort` 의 `finishing` 단계
- 자위 완료 직전 주변 확인 → 타인 존재 시 훅 발동
- 연인이든 비연인이든 **당사자 관점에서 수치심은 공통 발생**
- 후속 성욕 감소(연인 ½, 비연인 0)는 기존 로직 유지

#### 1.7.2 `on_nude_in_public` — 공공장소 노출 시

**위치**: `romance_core.py::_on_time_elapsed_shame` 에 `_check_nude_in_public_tick` 연결
- 1시간 간격 (기존 shame_decay와 동일 hook)
- 플레이어 위치의 같은 location에 있는 NPC들 순회
- 노출 상태(상체 or 하체) + 관객 있음(= 같은 location에 2명 이상) → 훅 발동
- 범위 한정: 전체 NPC 순회 대신 **플레이어 주변만** 체크 (비용 최소화)

**의도적 제한**:
- 플레이어 부재 시 (on_meet 트리거가 없는 상태) 공공 노출 감지 없음
- 전체 맵 NPC의 공공 노출 추적은 Phase 5 (NPC↔NPC 관계 행렬) 작업

#### 1.7.3 테스트

e2e `TestNudeInPublicTickHook` 3개:
- 노출 + 관객 → 수치심 증가
- NPC 단독 (플레이어 다른 location) → 수치심 변화 없음
- 플레이어 + NPC 2명만 있어도 발동 (플레이어가 "관객" 역할)

---

### Phase 1.8: 트랜스 상태 대사/수치 분기 (2026-04-24 완료)

**배경**: 트랜스 상태에서 NPC가 일반 캐릭터 톤을 유지하면 부자연스럽다.
깊은 트랜스는 유아퇴행·인간어 상실·의성어 위주. 또한 의식이 흐리므로
관계 기억(호감/반발)은 약화되고 몸(복종/쾌락/경험치)은 더 강하게 각인됨.

#### 1.8.1 수치 계수 (`compute_trance_multipliers`)

`romance_dynamics.py`에 트랜스 레벨별 효과 배율 함수 추가. `apply_effects`가
모드 배율(forced 등)과 **곱셈 합성**.

| 스탯 | 60~79 (trance) | 80+ (trance_deep) | 의미 |
|-----|---------------|-------------------|------|
| 호감 | ×0.6 | ×0.3 | 정신 기억 약화 |
| 반발 | ×0.6 | ×0.3 | 저항 의식 흐림 |
| 복종 | ×1.2 | ×1.5 | 몸 각인 강화 |
| 성욕/욕망 | ×1.1 | ×1.3 | 쾌락 루프 가속 |
| 절정 게이지 | ×1.2 | ×1.5 | 오르가즘 임박 가속 |
| 경험치 (부위별) | ×1.2 | ×1.5 | era 快楽 가속 (허들 낮추기) |

#### 1.8.2 era 快楽 호환성

era 시리즈의 `快楽` (쾌락 누적) 개념은 morld의 **기존 `경험:{부위}` + `복종`
할인 + 감도 배율 조합**으로 이미 표현 가능. 새 prop 불필요.

- 통합 쾌락 = `sum(경험:{부위})` — 필요 시 파생 함수로 제공
- 허들 낮추기 = Phase 0.5 `복종` 할인 + Phase 1 `get_sensation_level` 배율
- 조교 가시화 = 부위별 감도 레벨 상승

→ `era-series-analysis.md` §7.2의 "쾌락 누적 스탯 도입 고려"를 "**기존 경험치로 커버됨**"으로 갱신.

#### 1.8.3 대사 접두어 시스템

`_get_mode_reaction` 조회 순서 확장:

1. **트랜스 접두어 우선** (`trance_deep:{action}` > `trance:{action}`)
2. 모드 접두어 (`forced_{action}` 등)
3. 기본 반응
4. **공통 트랜스 fallback 풀** (`_GENERIC_TRANCE`, `_GENERIC_TRANCE_DEEP`)

공통 fallback 풀은 캐릭터 전용 trance 대사가 없을 때 사용. 의성어 위주:
- `_GENERIC_TRANCE`: "...하아... 아...", "...응... 헉..."
- `_GENERIC_TRANCE_DEEP`: "...아우... 으아...", "...히잉... 아앙..."

**캐릭터 샘플**: 유키에만 `trance:start` / `trance_deep:start` 추가.
나머지 5명은 후속 슬라이스 (캐릭터별 아키타입 톤으로 확장).

#### 1.8.4 테스트 커버리지

신규 8개 dynamics 테스트 (`TestTranceMultipliers`):
- 트랜스 0/60/80 구간별 배율 검증
- 임계값 정확히 판정
- 정신(호감/반발) 약화 vs 몸(복종/절정/경험) 증폭 분리

---

### Phase 1.9: 복종 고도화 → 자제심 침잠 (2026-04-24 완료)

**배경**: 함락된 NPC(복종 高)가 기본 자제심을 그대로 유지하면 부자연스럽다.
era 조교 서사 — "몸이 먼저 굴복하고 정신이 뒤따른다" — 를 반영하여 복종 누적 시 **실질 자제심**이 점차 무너지도록 설계.

#### 1.9.1 `get_effective_restraint(unit_id)` 파생 함수

```
raw = 성격:자제심 (아키타입 기본값 fallback)
erosion = max(0, 복종 - 60) × 0.75
effective = max(0, raw - erosion)
```

**감쇠 표** (기본 자제심 80 기준):
| 복종 | 감쇠 | 실질 자제심 | 효과 |
|-----|------|----------|------|
| 0~60 | 0 | 80 | 방어 유지 |
| 70 | -7.5 | 72 | 방어 약화 시작 |
| 80 | -15 | 65 | 방어 무너지는 중 |
| 100 | -30 | 50 | 방어 완전 소실 (함락) |

**복종 임계 60**: Phase 1.5의 `get_submission_label`이 "충성"으로 분류하는 지점.
라벨 전환이 실제 스탯 효과와 일치.

#### 1.9.2 적용 범위 (옵션 B — 트랜스 + availability)

**트랜스 방어** (`compute_trance_level`):
- 기존 `성격:자제심` 직접 조회 → `get_effective_restraint` 경유
- 함락 NPC는 트랜스 방어가 소실되어 자연 흥분만으로도 깊은 트랜스 진입 가능

**availability_score** (`get_restraint_modifier`):
- 자제심 점수 페널티도 실질 자제심 기반
- 함락되면 행위 점수 페널티 자연 감소 → 가용 행위 범위 확장
- 효과: 함락 NPC는 저항 없이 고난도 행위도 가능

**미적용 범위**:
- 수치심 모디파이어 (`get_shame_modifier`)에는 raw 자제심 유지 — 수치심과 함락은 축이 달라 독립 유지

#### 1.9.3 테스트 커버리지

신규 10개 dynamics 테스트:
- `TestEffectiveRestraint` 7개: 복종 구간별 감쇠 검증 (0/50/60/70/80/100/floor)
- `TestTranceInfluencedByCorruption` 3개: 함락 시 트랜스 방어 소실 → 깊은 트랜스 cascade

---

### Phase 1.9.1: 절정 ↔ 트랜스 연계 (2026-04-24 완료)

**배경**: 사용자 비유 — "높은 자제심으로 버티다가 연속 절정 시 일시 상실
→ 트랜스 → 회복기에 원상복귀 + 부끄러움". **신규 prop 없이 기존 것 재활용**.

#### 1.9.1.1 적용 3지점

**1) 누적 절정 경험 bonus** (`compute_trance_level`):
- 부위별 `경험:절정:{part}` 합산 × 0.3, cap 15
- base에 소폭 더해서 장기 함락 진행을 표현 (era `快楽` 허들 낮추기)

**2) 절정 발동 시 `트랜스:외부` +20**:
- 기존 세뇌/약물 축 재활용 — "의식 흐림"이라는 점에서 같은 카테고리
- 연속 절정 3회 = +60 → 고자제심(90) NPC도 트랜스 진입 가능
- 구현: `romance.py` 절정 처리부 + `npc_initiative.py` 동일

**3) 1시간 tick 자연 감쇠**:
- `_on_time_elapsed_shame`에 `_decay_trance_external_tick` 얹음
- `트랜스:외부` -10/h → 연속 절정 3회 후 ~6시간 후 원상복귀
- `_SHAME_REGISTRY` 재활용 (romance 이벤트 겪은 NPC 자동 포함)

#### 1.9.1.2 시뮬레이션 (고자제심 90 NPC)

| 시점 | `트랜스:외부` | 실질 트랜스 | 상태 |
|-----|-----------|---------|------|
| 평상시 | 0 | 10~15 | 방어 유지 |
| 절정 1회 | 20 | ~32 | 여전히 미진입 |
| 절정 2회 | 40 | ~52 | 미진입 (겨우) |
| 절정 3회 | 60 | ~72 | **진입** (trance entry) |
| 2시간 후 | 40 | ~52 | 이탈 시작 |
| 6시간 후 | 0 | 10~15 | 원상복귀 |

#### 1.9.1.3 탈출 공식 연계

`calculate_escape_chance`에 트랜스 페널티 명시적으로 추가:
- 트랜스 entry (60~79): `chance -= 0.15`
- 트랜스 deep (80+): `chance -= 0.30`

연쇄 절정/여운은 이미 `트랜스:외부` 가산으로 트랜스 수치를 올리므로,
페널티가 자연스럽게 "절정 중엔 저항 불가" 효과 달성.

#### 1.9.1.4 테스트 커버리지

신규 6개 dynamics 테스트:
- `TestClimaxExperienceBonus` 4개: 부위 합산/cap/구간별 기여
- `TestTemporaryRestraintLossViaExternal` 2개: 외부 가산으로 고자제심 뚫기 + 감쇠 후 이탈

#### 1.9.1.5 후속 (미구현)

- ~~트랜스 이탈 시 `on_post_trance_return` 훅~~ — **Phase 1.9.2에서 완료**
- 회복 후 캐릭터별 "방금... 내가 뭘..." 대사 풀 (캐릭터별 `post_trance:start` 키)

---

### Phase 1.9.2: 회복 후 부끄러움 훅 (2026-04-24 완료)

**배경**: Phase 1.9.1에서 `트랜스:외부` 감쇠로 자연 회복하는 구조를 만들었음.
이제 이탈 순간 "방금 내가 뭘..." 수치심 발동 훅을 연결.

#### 1.9.2.1 이탈 감지 (`update_trance_level`)

```python
prev = get_unit_prop(uid, "상태:트랜스") or 0
new_value = compute_trance_level(uid)
set_unit_prop(uid, "상태:트랜스", new_value)
if prev >= TRANCE_ENTRY and new_value < TRANCE_ENTRY:
    on_post_trance_return(uid, prev_peak=prev)
```

- 이전 수치가 TRANCE_ENTRY(60) 이상 AND 현재 미만인 프레임에서만 1회 발동
- 재진입/재이탈해도 각 이탈마다 훅 발동

#### 1.9.2.2 `on_post_trance_return(uid, prev_peak)`

- 깊은 트랜스(80+)에서 이탈 → 수치심 +25
- 일반 트랜스(60~79) 이탈 → 수치심 +15
- Phase 1 수치심 시스템 (`apply_shame`) 재활용 — reason="post_trance_return"

#### 1.9.2.3 테스트 커버리지

신규 5개 dynamics 테스트 (`TestPostTranceReturn`):
- 깊은 트랜스 이탈 → +25
- 일반 트랜스 이탈 → +15
- 상승 중/유지 중/애초에 미진입 → 훅 발동 안 함

#### 1.9.2.4 남은 후속

- ~~캐릭터별 `post_trance:start` 대사 풀~~ — **Phase 1.9.3에서 완료**
- ~~대사 트리거 (session 내 last_reaction 삽입)~~ — **Phase 1.9.3에서 완료**

---

### Phase 1.9.3: post_trance 대사 풀 + 세션 연결 (2026-04-24 완료)

**배경**: Phase 1.9.2 훅이 수치심만 올리고 대사가 화면에 노출되지 않던 상태를 마무리.

#### 1.9.3.1 캐릭터별 대사 풀

6 캐릭터 × `post_trance:start` 키 × 아키타입 톤:
- lina (cheerful): "...어? 나 방금 뭐 한 거지...?"
- yuki (timid): "...저... 뭘 하고 있었죠... 창피해요"
- ella (cold): "...잊어." 차갑게 말하지만 목소리 떨림
- sera (stoic): "...방심했군."
- mila (gentle): "...제가 이상했죠...? 죄송해요..."
- faye (proud): "...젠장. 없던 일이야."

#### 1.9.3.2 세션 연결 (Registry 패턴)

`on_post_trance_return`이 모듈 수준이라 session state 직접 접근 불가.
**Registry + pop** 패턴으로 엣지 정보 전달:

```python
_LAST_TRANCE_EXITS = {}  # unit_id -> {prev_peak, shame_gain}

# update_trance_level이 이탈 감지 시:
_LAST_TRANCE_EXITS[unit_id] = {"prev_peak": prev, "shame_gain": gain}

# session closure에서 소비:
def _maybe_append_post_trance_reaction(pid):
    info = pop_last_trance_exit(pid)
    if info and _get_mode_reaction("post_trance", "start"):
        state["last_reaction"] += "\n" + reaction
```

`update_trance_level` 호출 지점 3곳(`_check_npc_thrust_trance`, `_can_enter_autonomy`, `_check_npc_insertion_request`)에서 직후 `_maybe_append_post_trance_reaction(pid)` 호출.

#### 1.9.3.3 테스트 커버리지

- 캐릭터 대사 풀 검증 1개 (e2e)
- Registry 3개 (dynamics): 이탈 시 저장 / pop 후 비움 / 이탈 없을 때 비어있음

---

### Phase 1.9.4: 정신 교란 아이템 — 알코올 + 미약 통합 (2026-04-24 완료)

**배경**: `트랜스:외부` 축의 실제 원인(아이템/상태)을 연결. 원인별 독립 prop
설계로 다양한 지속/감쇠/서사 분기 가능.

#### 1.9.4.1 원인별 prop 분리

| 원인 | prop | 지속/감쇠 |
|-----|------|---------|
| 알코올 (술) | `상태:취기` (0~100) | 1h -5 (천천히 빠짐) |
| 미약 | `상태:미약` + `상태:미약남은시간` (6h 타이머) + `트랜스:외부` +30 | `남은시간` -1/h, 0 되면 미약 해제 |
| 절정 여운 | `트랜스:외부` +20 (Phase 1.9.1) | 1h -10 |
| 최면/마약 | (후속) | — |

#### 1.9.4.2 `compute_trance_level` 확장

```python
external = (트랜스:외부) + (상태:취기)   # 합산 external 기여
value = int(base + external)
```

취기는 독립 prop으로 보관 → UI 표시 / 술 특화 서사 분기 가능.
`트랜스:외부`는 여운/미약/최면/약물 공유 축.

#### 1.9.4.3 신규 아이템: `Wine`

- `assets/items/consumables.py::Wine`
- `call:drink:마시기` → `상태:취기` +15 (1잔)
- `call:mix_food:음식에 넣기` → 음식에 `상태:취기첨가` 표식 → NPC 섭취 시 +15

#### 1.9.4.4 기존 미약 통합

- `Aphrodisiac.use()` 복용 시 `트랜스:외부` +30 추가 (기존 `상태:미약` 유지)
- `base.py::eat_food` NPC 경로 동일 (미약 첨가 음식)
- `_decay_aphrodisiac_tick` 1h tick: `상태:미약남은시간` -1, 0 되면 `상태:미약` 해제

#### 1.9.4.5 감쇠 tick 통합

`_on_time_elapsed_shame` 1h tick에 4개 감쇠 함수 체인:
- `_decay_shame_tick` — 수치심 자연 감쇠
- `_decay_trance_external_tick` — 여운/약물 회복
- `_decay_drunk_tick` — 취기 분해 (술 천천히 빠짐)
- `_decay_aphrodisiac_tick` — 미약 타이머

모두 `_SHAME_REGISTRY` 재활용 (romance 경험 있는 NPC 범위).

#### 1.9.4.6 테스트 커버리지

신규 5개 dynamics 테스트 (`TestDrunkennessAxis`):
- 취기만 있어도 트랜스 기여
- `트랜스:외부` + `상태:취기` 합산
- 고자제심 NPC도 취기로 진입 가능
- 100 clamp / 누락 prop 기본값

#### 1.9.4.7 남은 후속
- ~~독주 / 마약 / 최면제~~ — **Phase 1.9.5에서 완료**
- 술 전용 서사 분기 (`drunk:start` 대사 키)
- 중독/내성 메커니즘

---

### Phase 1.9.5: 추가 정신 교란 아이템 3종 (2026-04-24 완료)

Wine 확장 — 효과 수치만 다르게, 기존 `상태:취기` / `트랜스:외부` 축 재활용.
별도 "저항 감쇠" 로직 불필요 (Phase 1.9.1 트랜스 페널티로 자동).

#### 1.9.5.1 아이템 카탈로그

| 아이템 | 축 | 가산 | 감쇠율 | 지속 | 효과 |
|------|------|-----|------|-----|-----|
| Wine | 상태:취기 | +15 | -5/h | ~3h | 가벼운 억제 해제 |
| StrongLiquor | 상태:취기 | +30 | -5/h | ~6h | 취기 고조 |
| Narcotic | 트랜스:외부 | +50 | -10/h | ~5h | 깊은 트랜스 직진 |
| Hypnotic | 트랜스:외부 | +40 | -10/h | ~4h | 중간 세기, 은밀 |

#### 1.9.5.2 공통 구조
- 각 아이템 `call:use`(또는 `drink`) + `call:mix_food` 2경로
- NPC 음식 섭취부 (`base.py::eat_food`)에서 해당 첨가 prop 처리
- 기존 `_decay_drunk_tick` / `_decay_trance_external_tick`이 자동 감쇠

#### 1.9.5.3 테스트 커버리지

신규 5개 e2e (`TestIntoxicantItemsCatalog`):
- 각 아이템 정의/카테고리 검증
- 복용+음식첨가 액션 모두 보유

---

### Phase 2.1: NPC↔NPC 정사 조우 다이얼로그 (2026-04-24 완료)

**배경**: NPC↔NPC 자율 성행위(`_handle_npc_intimacy`)와 플레이어 발각 반응은
이미 존재. 누락된 것은 **플레이어 선택 분기** — 은신 시도 / 물러남 / 목격.

**진입점 통일**: `events/__init__.py`가 meet 이벤트 → 은신 판정 내장 →
비은신 시 `on_meet_player` → `_on_npc_intimacy_discovered` 경로.
도착/은신해제/NPC 감지가 모두 이 함수를 지나가므로 여기서 분기만 확장.

#### 2.1.1 조우 다이얼로그 선택지

| 선택지 | 동작 |
|-------|-----|
| 몰래 숨는다 | `stealth.enter_stealth` + `detection_check` — 성공 시 세션 유지, 실패 시 목격 경로 fallthrough |
| 조용히 물러난다 | 세션 유지, 플레이어만 퇴장 |
| 목격한다 | 기존 발각 반응 (아키타입별 대사 + 세션 중단) |

리팩터: 발각 반응을 `_run_npc_intimacy_discovery_reaction`으로 분리 —
은신 실패/목격 공통 경로.

#### 2.1.2 기존 묘사 재활용

`_DESCRIBE_NPC_INTIMACY` (합의/강제 분기)가 이미 있음 — 은신 성공 후에도
NPC를 focus/describe하면 묘사 출력. 풍부한 관찰 묘사는 후속.

#### 2.1.3 Phase 2.2 후속 (사용자 요청)

- **개입 선택지** — 수치 조정(반발/호감) 중심. S02/S04 전투 시스템 차이로
  직접 전투는 **2차 연쇄**로만 발생
- **3P 참여** — 플레이어-NPC 연인 관계 시 조건부 노출
- **NTR 감정 연계** — 연인 NPC가 다른 NPC와 → 질투/배신 감정
- ~~풍부한 관찰 묘사~~ — **Phase 2.2a에서 완료**

---

### Phase 2.2a: 풍부한 관찰 묘사 (2026-04-24 완료)

**배경**: Phase 2.1 은신 성공 후 NPC를 focus/describe 시 "{name}(이)가
누군가와 얽혀 있다" 1줄만 출력. 매번 같은 대사라 단조로움.

#### 2.2a.1 `TextSelector.format_result` 확장

`result`가 **list**이면 `random.choice`로 1줄 선택 후 포맷.
기존 str 반환 경로는 그대로 — 회귀 없음.

```python
if isinstance(result, list) and result:
    result = random.choice(result)
# 기존 str 포맷 로직 계속
```

이 확장은 전역적 — 다른 `_DESCRIBE_*` 섹션도 list-of-strings 풀로 확장 가능.

#### 2.2a.2 `_DESCRIBE_NPC_INTIMACY` 풀 확장

| 조건 | 이전 | Phase 2.2a |
|-----|------|-----------|
| NPC강제피해중 | 1줄 | 8줄 (신음/저항/눈물/발버둥 등) |
| NPC성행위중 (합의) | 1줄 | 9줄 (얽힘/뜨거운 숨결/쾌락 떨림 등) |

매 describe 호출마다 랜덤 1줄 선택 → 단조로움 해소.

#### 2.2a.3 테스트 커버리지

신규 8개 e2e:
- `TestTextSelectorListChoice` 5개: str/list/빈 list/dict/randomness
- `TestNpcIntimacyDescribePool` 3개: 풀 구조 검증

---

### Phase 2.3: NTR 신뢰 훼손 (A방향 — 기존 수치로 연출) (2026-04-24 완료)

**배경**: NPC↔NPC 자율 정사 시스템이 이미 있어 "뺏기는 사실"은 자연 발생.
누락된 것은 **플레이어의 감정 반응**. era도 플레이어 감정 stat 없음 (Talent
플래그 + 이벤트 기반). morld는 기존 수치(호감/애정) 재활용으로 A 방향 선택.
B 방향(플레이어 감정 stat, `관계:{a}:질투:{b}` 행렬)은 Phase 5 후속.

#### 2.3.1 NTR 감지

`get_relationship_label(npc, player)` 결과가 **"연인"/"배우자"/"헌신적 종자"** 중 하나면 NTR 케이스.

#### 2.3.2 조우 다이얼로그 헤더 분기

| 상황 | 헤더 |
|-----|------|
| 합의 + 연인 | "당신의 연인인 {name}(이)가 다른 NPC와... 배신감이 엄습한다" |
| 강제 + 연인 | "당신의 연인인 {name}(이)가 강제로 당하고 있다! 분노가 치솟는다" |
| 합의 (비연인) | (기존 문구) |
| 강제 (비연인) | (기존 문구) |

#### 2.3.3 수치 반응

| 상황 | 효과 |
|-----|-----|
| 합의 + 연인 | 피해 NPC의 **플레이어 호감 -5, 애정 -5** (신뢰 훼손) |
| 강제 + 연인 | 변화 없음 (피해자는 잘못 없음, 가해자 반발은 Phase 2.2b에서) |
| 비연인 | 변화 없음 |

`_run_npc_intimacy_discovery_reaction`에서 처리 — 은신 실패/목격 경로 공통.

#### 2.3.4 남은 후속

- **Phase 2.2b 개입**: "그만두게 한다" / "참여한다" 수치 조정 중심, 2차 연쇄 전투
- **Phase 5 플레이어 감정 stat**: `관계:{a}:질투:{b}` / `관계:{a}:충성:{b}` 행렬

#### 2.3.5 테스트 커버리지

신규 3개 e2e (`TestNtrTrustPenalty`):
- 합의+연인 → 호감/애정 -5
- 강제+연인 → 변화 없음
- 비연인 → NTR 감지 안 됨

---

### Phase 2.4: 이름 부르기 (NTR/NTL 효과) (2026-04-24 완료)

**배경**: 트랜스/의식 흐린 상태에서 NPC가 속마음의 이름을 부르는 서사
연출. NPC↔NPC 정사 목격 시엔 NTR, 자위 목격 시엔 짝사랑 드러남.

#### 2.4.1 `_get_beloved_name(unit_id)` 파생

`관계:*:호감` prop 중 임계(`_BELOVED_AFFECTION_THRESHOLD = 40`) 이상의
**최고값 대상의 이름** 반환. None이면 사랑하는 이 없음.

명시적 소유 prop 없이 **수치 파생** — era 철학 + morld 기존 `_is_lover_npc`
패턴과 일관. 관계 변동에 따라 자연히 beloved도 변동.

#### 2.4.2 `_build_context`에 beloved 주입

```python
context["beloved"] = beloved or ""
context["beloved_exists"] = beloved is not None
```

rule 조건에 `{"beloved_exists": True}` 사용 → 조건부 풀 매칭.
Phase 2.2a의 list 랜덤 pick과 조합 — beloved 있으면 일반 대사 + 이름 부르기
대사 혼합 풀에서 랜덤 선택.

#### 2.4.3 풀 확장

**`_DESCRIBE_NPC_INTIMACY`** (Phase 2.2a 확장):
- 강제 + beloved → 기존 8줄 + "도와달라 {beloved}" 2줄
- 합의 + beloved → 기존 9줄 + "{beloved} 중얼거림" 3줄 (NTL 효과)

**`_DESCRIBE_MASTURBATION`** 신규:
- 일반 4줄 / beloved 있음 6줄 (3줄은 "{beloved}" 이름 부르기)
- `_DEFAULT_DESCRIBE_ORDER`에 `"masturbation"` 추가

#### 2.4.4 플레이어와 세션 중 — Phase 2.4.1에서 완료

세션 내 대사 경로(`_get_mode_reaction`)에 `{beloved}` 치환 post-process 추가:
- 캐릭터 asset의 `trance_deep:start` 풀에 `{beloved}` 포함 대사 추가
- `_substitute_beloved` closure 헬퍼:
  - `beloved == 플레이어 이름`이면 NTR/NTL 아님 → 공통 fallback 풀로 교체
  - 그 외면 치환 수행 (NTL 연출)
  - `beloved` 없으면 fallback

6 캐릭터 × 2줄 beloved 대사 추가 (아키타입 톤별):
- lina: "...{beloved}...♡ 아...♡"
- yuki: "...{beloved}... 죄송해요..."
- ella: "...{beloved}..." 낯선 이름 흘림
- sera: "...{beloved}..." 거친 숨결
- mila: "...{beloved}... 좋아요...♡"
- faye: "...{beloved}...!" 자존심 붕괴

#### 2.4.5 테스트 커버리지

신규 10개 e2e:
- `TestBelovedName` 6개: 임계/최고값/다중 관계/수치 외 무시
- `TestMasturbationDescribePool` 4개: 풀/순서/{beloved} 포함 대사

---

### Phase 2: 성향 탤런트 체계
**Talent 시스템 이식**
- 신규 네임스페이스: `성향:{key}` (영구 또는 느린 변동)
- 핵심 탤런트: 담력/태도/자존심/츤데레/정조/저항/쾌감응답/도착적/새드/마조/성별기호
- 아키타입별 기본 탤런트 매핑 (8개 캐릭터 * ~12 탤런트)
- 각 커맨드/게이트 공식에 탤런트 보정 계수 반영

### Phase 3: 행위 카테고리 확장
**현재 스킨쉽 액션 → Train.csv 370종 규모로 분류**
- 카테고리 체계 도입: 애무/도구/삽입/SM/강제/데이트/일상/특수
- 현재 `romance_actions.py`의 ACTION_DEFS 재설계
- 각 액션에 해금 조건 (호감/복종/자제심/탤런트/경험)
- 실행 효과 (탤런트별 보정, 부위별 경험 누적)

### Phase 4: 다인 시나리오 지원
**부위별 점유 모델**
- 신규 네임스페이스: `점유:{부위}:{속성}` — 사용정도/사용자
- `romance_scene` 상태 확장: 참가자 배열 + 부위 분담
- 새로운 액션 카테고리: 다인 전용 (69/파이즈리펠라/교대삽입 등)
- UI: 참가자 선택 및 부위 할당

### Phase 5: NTR/NTL + 플레이어 대칭 스탯
**morld 고유 확장 (era 미구현 영역)**
- 질투 탤런트 구현 (Talent 84)
- `관계:{a}:충성:{b}` / `관계:{a}:질투:{b}` 스탯
- 플레이어 본인 스탯 풀세트:
  - `플레이어:상태:성욕` / `플레이어:상태:수치심` / `플레이어:처녀:{부위}` / `플레이어:경험:{부위}`
  - `플레이어:성격:자제심` / `플레이어:성향:{key}`
- NTR 이벤트: 관찰자 목격 + 증거 시스템 + 발각 판정
- NTL: 명시적 허용 플래그 + 참여도 스탯
- 역방향 지배 (NPC가 플레이어를 대상으로 행위)

### Phase 6: S04 이식
**S02 완성 후 일반화**
- `scenarios/common/python/engine/romance/` 분리
- 엔진화된 스탯/게이트/액션 카테고리를 S04에 그대로 적용
- S04 고유 요소만 S04 측에 남김

---

## 6. 우선순위 판단

### 즉시 (이번 세션)
- Phase 0 — `관계:{p}:욕망` 제거

### 단기 (1~2주)
- Phase 1 — 자제심 · 수치심 도입
- Phase 2 시작 — 핵심 탤런트 5개 (자존심/정조/쾌감응답/새드/마조) 먼저

### 중기 (1개월~)
- Phase 2 완성 — 전체 탤런트 체계
- Phase 3 — 행위 카테고리 확장

### 장기
- Phase 4 — 다인 시나리오
- Phase 5 — NTR/NTL + 플레이어 스탯
- Phase 6 — S04 이식

---

## 7. 구현 결정 필요 항목

### 7.1 스탯 네임스페이스 확정
- **제안**: `성격:*` (영구 탤런트), `성향:*` (중기 변동), `상태:*` (순간), `관계:*` (관계)
- 혹은 era TW식 단일 `Talent[]` 배열 유지

### 7.2 탤런트 도입 방식
- **A. Prop 기반 동적** (현재 morld 스타일) — 실행 시 값 읽고 분기
- **B. 캐릭터 파일 하드코딩** — python 코드 내 상수
- **C. 하이브리드** — 기본값은 python, 런타임 변경은 prop

### 7.3 아키타입 ↔ 탤런트 매핑
- 현재 morld 6개 아키타입 × 핵심 탤런트 10개 기본값 표 작성 필요

### 7.4 다인 시나리오 UI
- 참가자 2인 → 3인+ 확장 시 어떤 UI 패턴? (era는 텍스트로 처리)

---

## 8. 참고 자료 위치

- 원본 소스: `c:\tmp\era-source\TWKR-Textbung`
- 핵심 파일:
  - `CSV/Palam.csv` / `Abl.csv` / `Talent.csv` — 스탯 정의
  - `CSV/Train.csv` — 커맨드 목록
  - `CSV/Mark.csv` / `CSV/Stain.csv` / `CSV/ex.csv` / `CSV/exp.csv`
  - `ERB/다인조교/나눠먹기.ERB` — 다인 메커니즘
  - `ERB/COMMON.ERB` (line 364 레즈비언 판정, 500-1041 우후후)
  - `ERB/ANOTHER_TALK.ERB` — NPC 간 상호작용
  - `ERB/잠복모드관련/`, `ERB/감금관련/`, `ERB/지하감옥/`, `ERB/지하매음굴/` — 특수 시나리오

## 9. 아크타입 요구사항 감사 (Top-Down Audit)

> 31개 아크타입 각각이 요구하는 시스템을 역방향 추적하여 **공통 베이스 / 클러스터 특화 / 불필요**를 분리.
> [romance-trajectory-analysis.md](romance-trajectory-analysis.md) Section 2 참조.

### 9.1 공통 베이스 — 모든 아크타입이 요구 (전원 필수)

이 요소들은 31개 전체 또는 대부분의 아크타입이 공유하므로 **Phase 0~1에 우선 구축**.

**스탯 (필수)**:
- `관계:{p}:호감` ✅ 현재 있음
- `관계:{p}:복종` ✅ 현재 있음
- `관계:{p}:반발` ✅ 현재 있음
- `상태:성욕` ✅ 현재 있음 (욕망 제거 후 게이트에 편입)
- `성격:자제심` ⭐ 신규 — Phase 1
- `상태:수치심` ⭐ 신규 — Phase 1
- `경험:{부위}` ✅ 현재 있음 (축소 사용)
- `처녀:{부위}` ✅ 현재 있음

**탤런트 (필수 — 최소 set)**:
- 성별기호 (이성애/양도/동성기피) — ⑮ 동성, 다수 오버레이
- 자존심/반항/정조 — 반항 클러스터 다수
- 쾌감응답 — 함락 가속/저항 제어
- 자위하기쉬움 — 자위 이벤트 빈도

**시스템 (필수)**:
- 3-path 게이트 공식 (호감/복종/성욕) — 전원
- 아키타입 톤 대사 자동 생성 (Hybrid 엔진) ✅ 현재 있음
- 플레이어 스탯 대칭 — 원칙 0.3 + ⑬⑭⑪ 등
- 각인 시스템 (쾌락/고통/반발/굴복) — 궤적 히스토리 기록

### 9.2 클러스터별 특화 요구

**클러스터 A: 합의·감정 중심 (①②③⑰⑲⑳)**
- 필요: `관계:{p}:애정` (상위 티어) — Phase 2
- 필요: 시작 배경 플래그 (소꿉친구/계약/거짓연인) — Phase 1
- 필요: 데이트 액션 카테고리 확장 — Phase 3
- 탤런트: 츤데레/연모/정조 — Phase 2

**클러스터 B: 욕정·충동 (④⑤⑥)**
- 필요: 환경 자극 이벤트 훅 (on_witness_masturbation, on_see_nude 등) — Phase 1
- 필요: 자제심 게이트 공식 반영 — Phase 1
- 필요: 약물/술 효과 (자제심 일시 감소) — Phase 1 후반

**클러스터 C: 강제·함락 (⑦⑧⑨⑱)**
- 필요: 각인 누적 임계 판정 — Phase 2
- 필요: 강제 액션 카테고리 (Train 120-150) — Phase 3
- 필요: 붕괴 상태 (Talent 28) — Phase 2
- 탤런트: 저항/자기애/통각/새드/마조/도착 — Phase 2

**클러스터 D: 제3자 (⑩⑪⑫)**
- 필요: **NPC↔NPC 관계 행렬** — Phase 5 — 지금까지의 "player-centric"에서 확장
- 필요: `관계:{a}:충성:{b}` / `관계:{a}:질투:{b}` 스탯
- 필요: 발각/목격 이벤트 (증거, 비디오촬영 등)
- 필요: Talent 84 질투 구현 (era는 정의만)

**클러스터 E: 대칭 (⑬⑭)**
- 필요: 플레이어 풀세트 스탯 (이미 원칙 0.3에 포함)
- 필요: NPC think가 플레이어를 대상으로 동작 — Phase 5
- 역방향 게이트 공식 (플레이어 stat 기준)

**클러스터 F: 시뮬 자생 (㉔㉕㉖㉗)**
- 필요: 경험치 → 탤런트 동적 획득 규칙 (㉔) — Phase 2
- 필요: NPC가 타 NPC 유혹 의지 (㉕) — Phase 5
- 필요: NPC↔NPC 독립 호감 시뮬레이션 (㉖) — Phase 5
- 필요: 3자 개입 이벤트 훅 (㉗) — Phase 5

**클러스터 G: 대안 결속 (㉘㉙)**
- 필요: 탤런트 감정결여/무관심 — Phase 2
- 필요: 임신 의도 플래그 (Talent 9 임신소망) — Phase 2

**클러스터 H: 판타지 (㉚㉛)**
- 필요: 서사 플래그 시스템 (정체 숨김, 조작 상태) — Phase 5+ or S04
- ㉚ 정신 조작: **S04로 이관** (morld 세계관에 과도)

**오버레이 (직교)**:
- 동성 (⑮): 성별기호 탤런트만 필요 — Phase 2에서 커버
- 다인 (3+): 부위 점유 모델 — Phase 4
- 임신: ✅ 현재 있음 (복수 자녀는 반복 임신으로 자연)
- 장소: 관객계수 (수치심 공식의 입력) — Phase 1

### 9.3 불필요 — 제거 또는 미도입

> **원칙 재확인**: morld는 ero 게임이 아니다. 연애 시뮬레이션의 **자연스러움**이 목표이며, era의 explicit 세밀화는 대부분 불필요. 다만 시뮬레이션 상호작용성을 높이는 세밀함은 유지.

**era에서 가져오지 않을 것**:
- TALENT 1075 전체 — morld는 **~30개 핵심만**
- exp.csv 85 항목 → morld는 **~20개만** (부위 + 주요 행위 + 체위별 절정 + 조교/출산)
- 무자각 경험 (60-65) — **제외**
- Mark 시간각인/성장 — Phase 5+로
- 연령 축 (-1 아이) — **제외, 성인만**
- 탄막 전투 / 카지노 — 별개 시스템, **제외** (morld는 고유 전투/경제)

**유지 — 상호작용적 시뮬레이션에 기여**:
- **체위별 절정경험 (101-110)**: 정상위/후배위/기승위/대면좌위/배면좌위 × V/A = 10종 유지.
  `경험:{체위}{orifice}절정`. 체위별 차별성이 행위 시스템의 재미 요소. 체위 해금/선호도 조건에 활용.
- **시간정지**: S02에 **이미 `frozen.md` 시스템 존재** (Frozen 상태). era와 다른 맥락이지만 핵심 기능은 비슷. 제거하지 않고 기존 시스템 활용. 향후 유사 기능 확장 여지.
- **후타나리 = 양기 보유 단순 플래그**: 별도 로직 분기 없음. 단순히 `기관:남성기=1, 기관:여성기=1` 플래그 동시 보유로 처리. 성별기호 탤런트는 별개로 동작. 특수 경로 아님.
- **감금/매음 — 별도 시스템 미구현, 기존 시스템 조합**:
  - 감금 = 위치 제한 (텔레포트 차단) + bed_owner 제거 + 행동 제약 플래그
  - 매음 = 화폐 거래 이벤트 + 평판 prop + 공공 장소 접근
  - 별도 "감옥 모듈" / "매음굴 모듈" 만들지 않고 **조합으로 자연 발생**

**기존 morld에서 제거할 것**:
- ~~`관계:{p}:욕망`~~ — Phase 0에서 제거 확정

**우선순위 낮춤 (Phase 5+ or 스킵)**:
- ㉚ 정신 조작 → S04로
- ㉛ 정체 숨김 → Phase 5+ 서사 플래그
- ⑨ 정신 붕괴 접근로 → Phase 2 구현, 기본 비활성
- 다인 (3+) → Phase 4

### 9.4 계보 시스템 (Lineage)

**출산 결과를 prop으로 영구 보존 + 계보 추적**.

현재 `pregnancy.py`의 부분 구현:
- 아이에 `부모:어머니` / `부모:아버지` prop 존재
- 어머니에 `상태:아이아버지` / `상태:아이아버지id` 존재 (임신 중 추적용)

**보강 설계**:

```
# 아이 관점 (출생 시 영구 설정)
관계:부:{father_name} = father_unit_id
관계:모:{mother_name} = mother_unit_id

# 부모 관점 (출산 시 추가, 다자녀 가능)
관계:자녀:{child_name} = child_unit_id  # 복수 prop 패턴 (bed_owner 다수 보유와 동일)
```

**시사점**:
- "다자녀" 오버레이는 별도 스탯 아님 → **자녀 prop 개수가 곧 자녀 수**
- 계보 추적 자동화: `get_children(unit_id)`, `get_parents(unit_id)` 헬퍼
- 근친 감지 가능 (같은 부모 공유 시 형제/자매)
- NTR 서사에서 친부 확정 가능 (`관계:부:*` 조회)
- 손자/세대 확장 자연 (자녀 NPC가 또 자녀 낳으면 계보 연장)

**구현 Phase**:
- Phase 0 또는 1에서 기존 `pregnancy.py` 출산 핸들러에 계보 prop 추가
- 비용 낮음 (기존 구조 확장)

### 9.5 최종 감사 결과 — Phase별 재정렬

위 분석에 기반해 **필수 최소 순서**를 재구성:

| Phase | 목표 | 커버하는 아크타입 | 핵심 작업 |
|---|---|---|---|
| **0** | 정리 | ① 순애 안정화 | 욕망 제거, 디버그 정리 |
| **1** | 이중 억제축 + 배경 플래그 | ① ② ③ ④ ⑤ ⑥ ⑦(부분) ⑰ ⑲ ⑳ | 자제심/수치심/환경 자극 훅/배경 플래그 |
| **2** | 탤런트 + 각인 + 애정 | ① ② ⑦ ⑧ ⑨ ⑱ ㉔ ㉘ ㉙ ⑮(성별기호) | 핵심 탤런트 ~30개, 각인 5종, 애정 티어 |
| **3** | 액션 확장 + 병리 | ⑦ ⑧ ㉑ ㉒ ㉓ | SM 카테고리, 병리 탤런트, 얀데레 이벤트 |
| **4** | 다인 시나리오 | 3+ 오버레이 | 부위 점유 모델 |
| **5** | 제3자 + 대칭 + 시뮬 자생 | ⑩ ⑪ ⑫ ⑬ ⑭ ㉕ ㉖ ㉗ ⑯ ㉛ | NPC↔NPC 행렬, 질투/충성, 플레이어 풀세트 |
| **6** | S04 이식 | ㉚ (S04에서만) | engine/ 분리 + S04 통합 |

### 9.6 결론

- **공통 필수**: 8 스탯 + 3-path 게이트 + 플레이어 대칭 + 각인 시스템 + 계보 prop
- **핵심 확장**: 자제심/수치심/애정/탤런트 ~30개
- **유지 결정**: 체위별 절정경험(상호작용), 시간정지(S02 기존), 후타나리(양기보유 플래그), 감금/매음(조합 발생)
- **제거 확정**: era 과도 세밀화(무자각/연령/탄막/카지노 등) + morld 욕망 prop
- **Phase 재정렬**: 0 → 1(억제축) → 2(탤런트/각인) → 3(액션확장) → 4(다인) → 5(제3자+대칭+자생) → 6(S04)

설계 공간은 정제됐지만, **구현 스코프 여전히 크다**. 각 Phase 진입 전 세부 체크리스트 별도 작성 권장.

---

## 10. 관련 morld 문서
- [era-series-analysis.md](era-series-analysis.md) — era 시리즈 전체 분석
- [romance.md](romance.md) — 현 연애 시스템
- [romance-relationship.md](romance-relationship.md) — 관계 축 현 구현
- [romance-actions.md](romance-actions.md) — 액션 현 구현
